import "@testing-library/jest-dom";

describe("Кнопка добавить", () => {
  it.todo("Должна блокироваться для строки больше 32 символов");
  it.todo("Должна блокироваться для строки меньше 1 символа");
});
